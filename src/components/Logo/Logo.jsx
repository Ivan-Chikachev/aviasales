import React from 'react';
import logo from '../../access/img/Logo.svg';
import './Logo.scss';

const Logo = () => (
        <div className="logo">
            <img src={logo} alt="" />
        </div>
    );

export default Logo;
